version: 1.0.{build}
branches:
  only:
  - release
skip_tags: true
clone_depth: 50
environment:
  nodejs_version: "12"
install:
- cmd: npm install
build_script:
- cmd: npm run build
test_script:
- cmd: npm run test_for_build_server
